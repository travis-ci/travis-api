require "rails_helper"

RSpec.describe SubscriptionsController, type: :routing do
  pending "a subscription route spec will exist eventually"
    # I think we will need this as the current admin has some routing redirects.

    # it "routes to #show" do
    #   expect(:get => "/subscriptions/1").to route_to("subscriptions#show", :id => "1")
    # end

end
